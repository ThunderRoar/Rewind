{
  "name": "@rewind/mcp-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Rewind MCP server — agents self-query their history (search_history, find_similar_failures, get_run).",
  "license": "Apache-2.0",
  "bin": {
    "rewind-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
